import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'freeGamesApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
